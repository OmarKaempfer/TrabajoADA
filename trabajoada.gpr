project Trabajoada is

   for Source_Dirs use ("src");
   for Object_Dir use "obj";
   for Main use ("main.adb");

   package Ide is
      for Vcs_Kind use "git";
   end Ide;

   package Compiler is
      for Switches ("ada") use ("-gnato", "-g", "-gnata", "-fstack-check", "-gnat12");
   end Compiler;

end Trabajoada;

